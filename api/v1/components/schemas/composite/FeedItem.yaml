type: object
title: "Feed Item"
description: |
  Composite object for blog feed containing post info + author details.
  Demonstrates field amalgamation from multiple sources.
properties:
  post_id:
    type: string
    format: uuid
    description: "Unique post identifier"
    x-based-on:
      schema: "../base/post/Post.yml"
      field: "id"
      field-rename: "post_id"
      last-verified: "2024-06-03"
  post_title:
    type: string
    description: "Post title"
    x-based-on:
      schema: "../base/post/Post.yml"
      field: "title"
      field-rename: "post_title"
      last-verified: "2024-06-03"
  post_excerpt:
    type: string
    description: "Short post summary"
    x-based-on:
      schema: "../base/post/Post.yml"
      field: "excerpt"
      field-rename: "post_excerpt"
      last-verified: "2024-06-03"
  post_published_at:
    type: string
    format: date-time
    description: "When the post was published"
    x-based-on:
      schema: "../base/post/Post.yml"
      field: "published_at"
      field-rename: "post_published_at"
      last-verified: "2024-06-03"
  post_like_count:
    type: integer
    description: "Number of likes on this post"
    x-based-on:
      schema: "../base/post/Post.yml"
      field: "like_count"
      field-rename: "post_like_count"
      last-verified: "2024-06-03"
  # Fields from User (author)
  author_id:
    type: string
    format: uuid
    description: "Post author's user ID"
    x-based-on:
      schema: "../base/user/User.yml"
      field: "id"
      field-rename: "author_id"
      last-verified: "2024-06-03"
  author_username:
    type: string
    description: "Post author's username"
    x-based-on:
      schema: "../base/user/User.yml"
      field: "username"
      field-rename: "author_username"
      last-verified: "2024-06-03"
  author_display_name:
    type: string
    description: "Post author's display name"
    x-based-on:
      schema: "../base/user/User.yml"
      field: "display_name"
      field-rename: "author_display_name"
      last-verified: "2024-06-03"
  author_avatar_url:
    type: string
    format: uri
    description: "Post author's avatar URL"
    x-based-on:
      schema: "../base/user/User.yml"
      field: "avatar_url"
      field-rename: "author_avatar_url"
      last-verified: "2024-06-03"