type: object
title: "Post Summary"
description: |
  Post summary for list views, feeds, and cards.
properties:
  id:
    type: string
    format: uuid
    description: "Unique post identifier"
    x-based-on:
      schema: "../Post.yaml"
      field: "id"
      last-verified: "2024-06-03"
  title:
    type: string
    description: "Post title"
    x-based-on:
      schema: "../Post.yaml"
      field: "title"
      last-verified: "2024-06-03"
  slug:
    type: string
    description: "URL-friendly post identifier"
    x-based-on:
      schema: "../Post.yaml"
      field: "slug"
      last-verified: "2024-06-03"
  excerpt:
    type: string
    description: "Short post summary/excerpt"
    x-based-on:
      schema: "../Post.yaml"
      field: "excerpt"
      last-verified: "2024-06-03"
  published_at:
    type: string
    format: date-time
    description: "Post publication timestamp"
    x-based-on:
      schema: "../Post.yaml"
      field: "published_at"
      last-verified: "2024-06-03"